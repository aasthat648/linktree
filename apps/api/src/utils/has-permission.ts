// import { Role, Permission, ROLE_PERMISSIONS } from '@/types/role-permissions';

// export const hasPermission = (role: Role, permission: Permission): boolean =>
//   ROLE_PERMISSIONS[role].includes(permission);
